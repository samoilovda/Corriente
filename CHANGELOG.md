# CHANGELOG

Формат — по релизам; внутри релиза записи сгруппированы по этапам плана разработки
(`docs/BUILD_PLAN.md`, `docs/FIX_PLAN.md`, `docs/ROADMAP.md`) и получены прямым разбором
истории коммитов (`git log`), не переписаны заново. Обозначения задач:

* **`Tx.y`** — `docs/BUILD_PLAN.md`, этапы 0–5 («учёт», «переводы», «импорт из Monefy»,
  «виджет», «полировка»).
* **`Fx.y`** — `docs/FIX_PLAN.md`, приоритеты P0–P3 (исправление дефектов после закрытия
  `BUILD_PLAN.md`).
* **`Rx.y`** — `docs/ROADMAP.md`, этапы R1–R6 (развитие после `FIX_PLAN.md`).

Правило версий — `README.md`, раздел «Версии»: `versionCode` растёт на 1 с каждым
релизным APK, `versionName` — семантический `MAJOR.MINOR.PATCH`.

## [0.2.0] — versionCode 2

Первый настоящий релиз: до этой версии `versionCode`/`versionName` ни разу не менялись
со значений по умолчанию (`1` / `"0.1"`), хотя приложение уже прошло этапы `BUILD_PLAN.md`
1–5 и весь `FIX_PLAN.md`.

### R6 — Релизная инженерия

- `f2c5582` **R6.1**: релизная сборка — `buildTypes.release` (`isMinifyEnabled`,
  `isShrinkResources`), правила R8 (`app/proguard-rules.pro`), `signingConfig` из
  `local.properties`/переменных окружения без отката на debug-подпись.
- **R6.2**: версии перенесены в `gradle/libs.versions.toml`, введено правило нумерации,
  заведён этот файл.
- **R6.3**: английская локализация UI (`values-en/strings.xml`), русские литералы вынесены
  из Kotlin-кода в ресурсы (`ImportViewModel`, `MonefyImportPlanner`, `ReportViewModel` и
  другие — полный список см. в самом коммите).
- **R6.4**: `LICENSE` (MIT), англоязычный раздел README, политика приватности,
  сверка зависимостей на отсутствие проприетарного/сетевого кода.

### R5 — Прочность

- `24cadc6` R5.4: устойчивость `TxnEntryViewModel` к пересозданию процесса (`SavedStateHandle`).
- `9d0106a` R5.3: отмена удаления операции — снекбар «Операция удалена — Отменить».
- `6f95315` R5.2: блокировка приложения (биометрия/PIN устройства).
- `5ff3a47` R5.1: инструментальные UI-тесты (ввод расхода, F0.3, восстановление бэкапа,
  нижняя навигация).

### R4 — Виджет и быстрый ввод

- `a5de4e3` R4.4: ярлыки приложения (app shortcuts).
- `9ef96b9` R4.3: ручной выбор категорий виджета.
- `53a4ebf` R4.2: доход и выбор счёта в быстром вводе из виджета.
- `cf46e74` R4.1: отзывчивые размеры виджета (`SizeMode.Responsive`).
- `e0fa917` R4.0: обновление виджета сразу после размещения при запущенном приложении.

### R3 — Отчёты

- `aeeb44a` R3.4: сколько стоили конвертации.
- `608d3d0` R3.3: экспорт отчёта и списка операций в CSV.
- `3d50354` R3.2: динамика по счёту.
- `b10dd29` R3.1: сравнение с предыдущим периодом.
- `299acb3` R3.0: цвет счёта в строках операций.

### R2 — Ежедневный сценарий

- `3b44d4a` R2.4: повторяющиеся операции.
- `a83c4e1` R2.3: бюджеты по категориям.
- `36503b7` R2.2: шаблоны быстрого ввода («частые»).
- `e9380a0` R2.1: быстрый полнотекстовый поиск по заметкам.

### R1 — Бэкап, которому можно доверять

- `ffeb21b` R1.5: плашка «Последний бэкап: N дней назад».
- `eb1159a` R1.4: проверка бэкапа — восстановление во временную in-memory БД.
- `07de449` R1.3: восстановление прямо из папки автобэкапа.
- `4bd9afa` R1.2: отправить бэкап через системный шеринг.
- `d6b1e6a` R1.1: документация по бэкапу в облако и пояснение про SAF-дерево Диска.

### F3 — приоритет P3 (полировка и мёртвый код)

- `9191a57` F3.4: мёртвый код и предупреждения lint.
- `aa4b4d4` F3.3: цвет и иконка счёта — редактор и список; правка их не затирает.
- `82442ba` F3.6: доли категорий в отчёте дают ровно 100 %.
- `16af43e` F3.5: Activity быстрого ввода не экспортированы, пакет виджета из контекста.
- `0e3ec08` F3.2: явные `dataExtractionRules` и `backup_rules` — всё исключено.
- `6589c96` F3.1: адаптивная иконка приложения.

### F2 — приоритет P2 (производительность и незавершённый UX)

- `5536cc5` F2.8: калькулятор показывает отрицательный результат, а не обнуляет молча.
- `145a1a3` F2.7: индикатор занятости при экспорте и восстановлении.
- `1282fba` F2.6: категории при вводе — сетка «иконка + подпись + цвет», не лента.
- `1c52d96` F2.5: не мигаем «нет счетов», пустое состояние ввода — действие.
- `8d99b3c` F2.4: edge-to-edge и системные вставки.
- `81aa7b7` F2.3: тёмная тема окна до первого кадра, day/night у прозрачных Activity.
- `622e4be` F2.2: виджет пересчитывает месячный итог на смене суток.
- `30489c6` F2.1: агрегация балансов в SQL, подписка на диапазон дат вместо всей таблицы.

### F1 — приоритет P1 (инварианты, бэкап, импорт)

- `056b5ec` F1.5: откат импорта Monefy доступен пользователю; миграция схемы v1→v2.
- `661fd49` F1.4: восстановление из бэкапа — проверка целостности и копия «до».
- `89bc879` F1.3: автобэкап — стабильное расписание, статус последнего запуска, ротация.
- `4066110` F1.2: импорт Monefy проходит проверки записи через общую `requireValidTxn`.
- `46e89fb` F1.1: итоги дня и доли отчёта считаются через `Math.*Exact`.

### F0 — приоритет P0 (падения и тихая порча данных)

- `a7a1691` F0.6: быстрый ввод из виджета не теряет операцию при пересоздании Activity.
- `c37395c` F0.5: импорт Monefy не пишет операции в счёт чужой валюты.
- `df811e9` F0.4: правка операции с архивной категорией сохраняет категорию.
- `1c949ae` F0.3: правка операции на архивном счёте остаётся на том же счёте.
- `da5b98c` F0.2: пути записи обрабатывают ошибки репозитория, снекбар вместо падения.
- `1d35b36` F0.1: ограничение длины ввода суммы, `toMinorOrNull` не роняет кадр.
- `2e6e7a7` docs: `FIX_PLAN.md` и `ROADMAP.md` — план исправления дефектов после закрытия
  `BUILD_PLAN.md`.

### T5 — Этап 5, полировка

- `5863996` T5.5: иконки/цвета категорий, Material You, тёмная тема, калькулятор.
- `86bec0f` T5.4: отчёт «мои фактические курсы обмена».
- `58b7a4d` T5.3: графики отчёта на Compose Canvas.
- `70d56dd` T5.2: поиск и фильтры по операциям.
- `41841a7` T5.1: автобэкап по расписанию + копия БД перед миграцией.
- `68292f5` chore: `verifyInvariantGuards` → `buildSrc/InvariantGuards` + юнит-тест (закрывает T0.5).
- `e56e90c` fix: ввод суммы — отрицательные промежуточные значения не роняют экран.
- `7cd1846` fix: нижняя навигация скрыта на под-экранах.

### T4 — Этап 4, виджет

- `7713b60` T4.4: настройки виджета — закрепление валют и активный счёт.
- `a02bbb8` T4.3: быстрый ввод расхода из виджета.
- `25b11ef` T4.2: виджет на Glance + пуш снимка после каждой записи.
- `1b350da` T4.1: снимок для виджета — чистый построитель + носитель в DataStore.

### T3 — Этап 3, импорт из Monefy

- `5224821` T3.5: приёмка импорта на контрольных суммах.
- `5dbe146` feat: T3.3 — ручное разрешение `NEEDS_REVIEW` на экране импорта Monefy.
- `191945c` T3.4: запись батча импорта Monefy, идемпотентность, откат.
- `9b63000` T3.3: dry-run экран импорта Monefy.
- `f812aae` T3.2: классификация строк Monefy и склейка переводов.
- `4ade446` T3.1: парсер CSV Monefy.

### T2 — Этап 2, переводы

- `9bda946` T2.3: перевод исключён из доходов/расходов и отчёта по категориям.
- `86769e8` T2.2: экран перевода — два счёта, две суммы, выведенный курс.
- `12a3770` T2.1: модель перевода — `updateTransfer` + вывод курса сделки.

### T1 — Этап 1, учёт

- `f8f85ff` T1.5a: быстрый ввод — сплит-FAB «−» расход / «+» доход.
- `d08a03b` T1.9: бэкап — экспорт/восстановление всей БД через SAF.

Более ранние задачи этапа 0 («фундамент», `docs/BUILD_PLAN.md` §2) и T1.1–T1.8 не оставили
отдельных коммитов с префиксом `Tx.y` в этой истории — репозиторий начинается с `d08a03b`.
