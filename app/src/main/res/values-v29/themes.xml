<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- F2.3: API 29+ - платформенная тема окна сама следует системному day/night,
         поэтому один родитель DayNight вместо пары values/ + values-night/.
         У Theme.DeviceDefault.DayNight нет .NoActionBar-варианта - убираем ActionBar явно. -->
    <style name="Theme.Corriente" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.Corriente.Translucent" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
